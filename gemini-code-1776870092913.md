# UPDATE DIRECTIVE: OmniChat Product & Business Logic

**Context:** The base Next.js UI and database schema are established. Now, we must implement the core business logic, monetization strategy, and UI adjustments tailored for our target demographic (University Students). 
Analyze the existing codebase and implement the following updates:

## 1. Authentication (Zero-Friction Auth)
* **Action:** Implement `next-auth` (Auth.js v5 if possible, or v4).
* **Providers:** Strictly **Google OAuth** and **Telegram Login**.
* **Constraint:** Remove any traditional Email/Password registration logic from the UI and DB schema if it was generated. Users must log in with 1 click.

## 2. Monetization Strategy (Antilopay & Microtransactions)
* **Concept:** No recurring monthly subscriptions. Students buy on impulse or for a specific exam period.
* **Update DB & UI:** Implement two types of purchasable items via the Antilopay gateway:
  1. **Token Packages (Micro-transactions):** E.g., "Экстренный пакет: 50,000 токенов" for instant top-ups.
  2. **Session Passes (Time-limited):** 7-day, 14-day, or 30-day "PRO Passes" that grant unlimited access to premium models *without auto-renewal*.
* **Task:** Create a sleek billing modal/page where these options are presented.

## 3. The Killer Feature (Photo-to-Solution) & Free Tier Limits
* **Mobile UI Update:** On mobile viewports, the file attachment button (Camera icon) in the input field must be **massive and highly prominent**. 
* **Auto-Routing Logic:** When a user uploads a photo, automatically select the best Vision model (e.g., Claude Sonnet 4.5) and append a hidden system prompt instructing it to extract text/formulas and solve the problem.
* **Paywall Trigger:** The Free tier (Llama 3) *strictly prohibits* image uploads. If a free user clicks the Camera icon or drops an image, immediately intercept it and show a beautifully designed Glassmorphism modal: "Для работы с фото нужна умная модель. Купите пакет токенов за 99 руб."

## 4. Viral Growth (Referral Loop)
* **DB Update:** Add a `referralCode` to the `User` model.
* **UI Action:** Add a highly visible banner/button in the sidebar and profile section: "🎁 Пригласи друга — получите по 10,000 PRO-токенов". 
* **Logic:** Generate a unique invite link for each user. When a new user signs up via this link, credit both accounts with tokens.

## 5. PWA (Progressive Web App) & Verification
* **Action:** Configure the Next.js app as a PWA so students can "Add to Home Screen" on iOS/Android. Add a `manifest.json` and basic service worker.
* **Infra:** Ensure there is a mechanism to serve an Antilopay domain verification file (e.g., a `.txt` or `.html` file) from the `/public` directory.

**Execution:** Please plan these changes, update `schema.prisma` (run `npx prisma db push` or generate migration if needed), install required packages (`next-auth`), and refactor the UI components accordingly.